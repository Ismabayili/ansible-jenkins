- name: Update all packages and install Nginx on ubuntu
  hosts: all
  become: yes
  vars:
    ansible_python_interpreter: /usr/bin/Python 3.10.7
  tasks:
#    - name: Update all packages
 #     apt:
  #      name: '*'
   #     state: latest
    - name: Install Nginx
      apt:
        name: nginx
        state: present
